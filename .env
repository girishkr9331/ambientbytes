PORT=3001
NODE_ENV=development

# Auth — used by seed-user.js to create the initial account
# (credentials are stored in blog.db, not here after seeding)
AUTHOR_USERNAME=admin
AUTHOR_PASSWORD=admin123

# JWT secret — change this to a long random string in production
JWT_SECRET=ambientbytes-super-secret-jwt-key-change-me-in-prod
